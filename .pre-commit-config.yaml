repos:
  - repo: local
    hooks:
      - id: black
        name: black
        stages: [commit]
        language: system
        entry: black --line-length=120 swagger2locustio
        types: [python]
        pass_filenames: false

      - id: pylint
        name: pylint
        stages: [commit]
        language: system
        entry: pylint --rcfile=setup.cfg swagger2locustio
        types: [python]
        pass_filenames: false

      - id: mypy
        name: mypy
        stages: [commit]
        language: system
        entry: mypy swagger2locustio
        types: [python]
        pass_filenames: false

      - id: pytest
        name: pytest
        stages: [commit]
        language: system
        entry: pytest swagger2locustio/tests
        types: [python]
        pass_filenames: false

      - id: pytest-cov
        name: pytest
        stages: [push]
        language: system
        entry: pytest --cov=swagger2locusio --cov-fail-under=100 swagger2locustio/tests
        types: [python]
        pass_filenames: false
